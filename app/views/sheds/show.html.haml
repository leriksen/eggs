.page-header
  = link_to sheds_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_shed_path(@shed), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1= "Shed ##{@shed.id}"

%dl.dl-horizontal
  %dt Name:
  %dd= @shed.name

