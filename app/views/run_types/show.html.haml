.page-header
  = link_to run_types_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_run_type_path(@run_type), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1= "Run Type ##{@run_type.id}"

%dl.dl-horizontal
  %dt Type:
  %dd= @run_type.type
